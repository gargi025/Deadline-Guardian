from datetime import datetime, timedelta


def estimate_start_time(deadline: str):
    """
    Temporary scheduling helper.

    Later this will use Google Calendar.
    """
    return f"Start well before {deadline}"
